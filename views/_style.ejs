<style>
    body {
            font-family: Arial, sans-serif;
            background: linear-gradient(to bottom, rgb(247, 245, 245), #c7e3e8, #f3f5f7 , #faf8f8, #887070);
            margin: 0;
            padding: 0;
            display:flexbox;
            justify-content: center;
            align-items: center;
            min-height: 120vh;
        }

        .container {
            width: 400px;
            padding-top: 100px;
            padding: 20px;
            background: linear-gradient(to bottom, rgb(5, 5, 227), #f4f5f2, #faf8f7);
            box-shadow: 0px 10px 10px rgba(207, 216, 192, 0.2);
            border-radius: 5px;
            text-align: center;
            margin-top: 20px;
        }

        h1 {
            font-size: 28px;
            margin-bottom: 20px;
            color: #333;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .input-group {
            margin-bottom: 20px;
        }

        label {
            display: block;
            font-size: 11px;
            color: #555;
            margin-bottom: 5px;
        }

        input[type="text"],
        input[type="date"],
        input[type="url"],
        textarea {
            width: 100%;
            padding: 10px;
            border: 1px solid #090000;
            border-radius: 5px;
            font-size: 16px;
        }

        button {
            background-color: #007bff;
            color: #fff;
            border: none;
            padding: 10px 20px;
            font-size: 18px;
            border-radius: 5px;
            cursor: pointer;
        }

        button:hover {
            background-color: #0056b3;
        }



        /* NAVBAR EJS GUEE */
    .navbar-container {
        text-align: center;
        margin-top: 20px;
    }

    .navbar-menu a {
        text-decoration: none;
        font-size: 18px;
        margin: 0 20px;
        color: #333;
        transition: all 0.3s ease;
    }

    .navbar-menu a:hover {
        color: rgb(37, 5, 244);
        text-shadow: -2px 2px 2px rgba(0, 0, 0, 0.5);
    }

    /* form add toko */
    .containerForm {
      text-align: center; 
      margin-top: 20px; 
    }
    form {
      display: inline-block; 
      text-align: center;

    }
    .center-button {
      text-align: center; 
    }

</style>